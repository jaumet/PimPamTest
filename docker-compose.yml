services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: pimpamtest
      POSTGRES_USER: pimpamtest
      POSTGRES_PASSWORD: pimpamtest
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
